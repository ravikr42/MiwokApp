language: android
sudo: required
jdk: oraclejdk8

before_cache:
 - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
 - rm -fr $HOME/.gradle/caches/*/plugin-resolution/


android:
  components:
    - tools
    - platform-tools
    - tools
    - build-tools-26.0.2
    - android-26
  
  notifications:
    email:
    - ravikr42@gmail.com
